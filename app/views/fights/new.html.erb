<h1>Let's FIGHT !</h1>

<%= form_with(model: @fight, local: true) do |form| %>
<% if @fight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fight.errors.count, "error") %> prohibited this fight to happen:</h2>

      <ul>
      <% @fight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div>
        <%= form.label :warrior1, 'Fighter 1:'  %>
        <%= form.collection_select :warrior1_id, Warrior.all, :id, :name %>
    </div>
    
    <div>
        <%= form.label :warriorpar2, 'Fighter 2:' %>
        <%= form.collection_select :warrior2_id, Warrior.all, :id, :name %>
    </div>
    
    <div>
        <%= form.submit "Fight" %>
    </div>
<% end %>